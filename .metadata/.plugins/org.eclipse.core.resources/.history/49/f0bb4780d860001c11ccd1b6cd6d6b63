package br.com.spring.main.model;

public class Archive {

    private String name;
    private String directory;

    public Archive(String fullDiretory) {
        return extractDataFromFullDiretory(fullDiretory);
    }

    private String extractDataFromFullDiretory(String fullDiretory) {
        if (fullDiretory.isEmpty())
            return "rola";
        
        return "não rola";
    }

}
